import { ExpertisesFilterPipe } from './expertises-filter.pipe';

describe('ExpertisesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpertisesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
