import { NameEmailFilterPipe } from './name-email-filter.pipe';

describe('NameEmailFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NameEmailFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
