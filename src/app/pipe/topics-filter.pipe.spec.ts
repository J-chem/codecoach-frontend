import { TopicsFilterPipe } from './topics-filter.pipe';

describe('TopicsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
